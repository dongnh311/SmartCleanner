<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MacCleaner.Win.App.Views.MyToolsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:MacCleaner.Win.App.Controls">

    <Grid RowSpacing="16" Padding="0,0,0,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <controls:ModuleHeader
            x:Name="Header"
            Grid.Row="0"
            Title="My Tools"
            Subtitle="Pinned shortcuts and folders"
            IconGlyphChar="&#xE8A4;" />

        <Grid Grid.Row="1" Margin="16,0,16,0" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="LabelBox"  Grid.Column="0" PlaceholderText="Label" />
            <TextBox x:Name="TargetBox" Grid.Column="1" PlaceholderText="C:\path\to\anything (file, folder, URL)" />
            <Button x:Name="AddButton" Grid.Column="2" Content="Add" Click="AddButton_Click" />
        </Grid>

        <Border Grid.Row="2" Style="{StaticResource Card}" Margin="16,0,16,0">
            <ListView x:Name="ItemsList" SelectionMode="None" />
        </Border>
    </Grid>
</Page>
