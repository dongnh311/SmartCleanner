<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MacCleaner.Win.App.Views.UpdaterPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:MacCleaner.Win.App.Controls">

    <Grid RowSpacing="16" Padding="0,0,0,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <controls:ModuleHeader
            Grid.Row="0"
            Title="Updater"
            Subtitle="Pending updates via winget"
            IconGlyphChar="&#xE777;" />

        <StackPanel Grid.Row="1" Margin="16,0,16,0" Orientation="Horizontal" Spacing="12">
            <Button x:Name="RefreshButton" Content="Refresh" Click="RefreshButton_Click" />
            <TextBlock x:Name="StatusText" VerticalAlignment="Center" FontSize="12" Opacity="0.65" />
        </StackPanel>

        <Border Grid.Row="2" Style="{StaticResource Card}" Margin="16,0,16,0">
            <ListView x:Name="UpgradesList" SelectionMode="None" />
        </Border>
    </Grid>
</Page>
