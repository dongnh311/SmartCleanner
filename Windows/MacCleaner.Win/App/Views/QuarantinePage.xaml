<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MacCleaner.Win.App.Views.QuarantinePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:MacCleaner.Win.App.Controls">

    <Grid RowSpacing="16" Padding="0,0,0,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <controls:ModuleHeader
            x:Name="Header"
            Grid.Row="0"
            Title="Quarantine"
            Subtitle="Held-back files awaiting restore or purge"
            IconGlyphChar="&#xE72E;" />

        <StackPanel Grid.Row="1" Margin="16,0,16,0" Orientation="Horizontal" Spacing="12">
            <TextBlock x:Name="PathLabel" VerticalAlignment="Center" FontSize="11" Opacity="0.6" />
            <Button x:Name="PurgeButton" Content="Purge &gt; 7 days" Click="PurgeButton_Click" />
        </StackPanel>

        <Border Grid.Row="2" Style="{StaticResource Card}" Margin="16,0,16,0">
            <ListView x:Name="RecordsList" SelectionMode="None" />
        </Border>
    </Grid>
</Page>
