<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MacCleaner.Win.App.Views.DuplicatesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:MacCleaner.Win.App.Controls">

    <Grid RowSpacing="16" Padding="0,0,0,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <controls:ModuleHeader
            Grid.Row="0"
            Title="Duplicates"
            Subtitle="SHA-256 streaming match (size pre-filter)"
            IconGlyphChar="&#xE8C8;" />

        <Grid Grid.Row="1" Margin="16,0,16,0" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="PathBox" Grid.Column="0" PlaceholderText="Path to scan" />
            <Button x:Name="ScanButton" Grid.Column="1" Content="Scan" Style="{ThemeResource AccentButtonStyle}" Click="ScanButton_Click" />
        </Grid>

        <Border Grid.Row="2" Style="{StaticResource Card}" Margin="16,0,16,0">
            <Grid RowSpacing="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock x:Name="StatusText" Grid.Row="0" FontSize="12" Opacity="0.65" />
                <ListView x:Name="GroupsList" Grid.Row="1" SelectionMode="None" />
            </Grid>
        </Border>
    </Grid>
</Page>
