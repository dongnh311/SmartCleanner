<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MacCleaner.Win.App.Views.ClockPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:MacCleaner.Win.App.Controls">

    <Grid RowSpacing="16" Padding="0,0,0,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <controls:ModuleHeader
            Grid.Row="0"
            Title="Clock"
            Subtitle="World clock — pinned time zones"
            IconGlyphChar="&#xE121;" />

        <Grid Grid.Row="1" Margin="16,0,16,0" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <AutoSuggestBox x:Name="TzBox" Grid.Column="0" PlaceholderText="Filter time zones…" TextChanged="TzBox_TextChanged" />
            <Button x:Name="AddTzButton" Grid.Column="1" Content="Pin" Click="AddTzButton_Click" />
        </Grid>

        <Border Grid.Row="2" Style="{StaticResource Card}" Margin="16,0,16,0">
            <ListView x:Name="ClocksList" SelectionMode="None" />
        </Border>
    </Grid>
</Page>
