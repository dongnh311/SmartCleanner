<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MacCleaner.Win.App.Views.ProcessMonitorPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:MacCleaner.Win.App.Controls">

    <Grid RowSpacing="16" Padding="0,0,0,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <controls:ModuleHeader
            x:Name="Header"
            Grid.Row="0"
            Title="Process Monitor"
            Subtitle="Live process snapshot, refreshed every 2 seconds"
            IconGlyphChar="&#xE9D5;" />

        <Grid Grid.Row="1" Margin="16,0,16,0" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox
                x:Name="FilterBox"
                Grid.Column="0"
                PlaceholderText="Filter by name…"
                TextChanged="FilterBox_TextChanged" />
            <TextBlock
                x:Name="CountText"
                Grid.Column="1"
                VerticalAlignment="Center"
                Opacity="0.65"
                FontFamily="Cascadia Mono,Consolas,Courier New"
                FontSize="12" />
        </Grid>

        <Border Grid.Row="2" Style="{StaticResource Card}" Margin="16,0,16,0">
            <ListView
                x:Name="ProcessList"
                SelectionMode="Single"
                IsItemClickEnabled="False">
                <ListView.HeaderTemplate>
                    <DataTemplate>
                        <Grid ColumnSpacing="12" Padding="12,8,12,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*" />
                                <ColumnDefinition Width="60" />
                                <ColumnDefinition Width="100" />
                                <ColumnDefinition Width="3*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="NAME"   FontWeight="SemiBold" FontSize="11" Opacity="0.55" />
                            <TextBlock Grid.Column="1" Text="PID"    FontWeight="SemiBold" FontSize="11" Opacity="0.55" />
                            <TextBlock Grid.Column="2" Text="MEMORY" FontWeight="SemiBold" FontSize="11" Opacity="0.55" />
                            <TextBlock Grid.Column="3" Text="PATH"   FontWeight="SemiBold" FontSize="11" Opacity="0.55" />
                        </Grid>
                    </DataTemplate>
                </ListView.HeaderTemplate>
            </ListView>
        </Border>
    </Grid>
</Page>
